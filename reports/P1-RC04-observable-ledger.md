# P1-RC04 Observable ledger and figure audit

Status: machine audit passes for all current Paper 1 outputs.

`data/manifests/paper1_observable_ledger.csv` assigns 11 figures, tables,
experiments and conclusion boundaries to direct attraction, elastic/potential
response, height response and total terrestrial observable. It records units,
instrument-comparison authorization, required public label and release
disposition.

All five current production figures are covered by the ledger. Frequency,
matrix, distance, sensitivity and frequency-requirement captions now identify
their P1-E006/P1-E008 source as direct radial gravity. Helgoland remains the one
component-resolved validation row: direct attraction, combined CE elastic
gravity and vertical displacement are stored separately. No six-process total
terrestrial observable is claimed. P1-E003 is explicitly a supplemental direct
`Tzz` gravity-gradient foundation and cannot consume vertical-gravity curves.

The audit rejects missing artifacts, blank units or observable fields,
unledgered figures and any production label containing unqualified `gravity`
without `direct`, `elastic`, `gradient` or `total`. Machine result:
`reports/p1_observable_ledger_audit.json`.
