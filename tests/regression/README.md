# Regression tests

Only compact, reviewed numerical fixtures belong here. Every fixture must record the generating Git commit, configuration, data manifest, expected tolerance, and reason for freezing it.

