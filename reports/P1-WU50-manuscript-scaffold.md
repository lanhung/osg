# P1-WU50 claim-safe manuscript scaffold

Status: methods-first LaTeX source created; scientific Results intentionally pending

The manuscript now contains the locked title and increment, observable/sign
conventions, planar/spherical/WGS 84 geometry, six-process table, SNR and
frequency-coverage rules, evidence-aware parameter semantics, validation scope,
data/code policy and explicit limitations.

All unsupported results are represented by `\AtlasPending{}` gates. Tests forbid
the two Paper 1 novelty claims prohibited by `claims.yml` and require the
engineering status of P1-E001--E004 to remain visible. A separate figure manifest
ties each planned panel to registered inputs and identifies missing scientific
evidence.

The current host lacks LaTeX, so successful PDF compilation is not claimed. The
CPU image must install and pin the TeX toolchain before the manuscript build can
become a registered workflow.
